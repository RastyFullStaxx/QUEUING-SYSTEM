VITE_API_BASE_URL=http://localhost:8080
# Queue monitor iframe preview URL.
# Update this to your deployed monitor URL (example: https://monitor.yourdomain/display).
# For local dev, ensure the queue-monitor app is running at this exact address.
VITE_QUEUE_MONITOR_URL=http://127.0.0.1:5175/display
